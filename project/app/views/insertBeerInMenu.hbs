{{#extend "body"}}
<div class="site-wrapper">

    <div class="site-wrapper-inner">

        <div class="cover-container">

            <div class="masthead clearfix">
                <div class="inner">
                    <nav>
                        <ul class="nav masthead-nav" style="margin-right:-20em;">
                            <li class="active"><a href="/">Home</a> </li>

                            {{#unless logged}}
                            <li class="active"><a href="/login">Login</a></li>
                            <li class="active"><a href="/register">Register</a>
                                {{/unless}}
                                {{#if logged}}
                                <li class="active"><a href="/logout">Logout</a></li>
                                {{/if}}
                            </ul>
                        </nav>
                    </div>
                </div>

                <div class="inner cover">
                    <h1 class="cover-heading">Select beers to add to the menu</h1>
                    <img class="profile-img" src="http://thewanderinghousewife.com/wp-content/uploads/2013/11/beer-2pint-tap-hand-circ-051214.png" alt="">
                    {{#if message}}
                    <h3 class="text-center text-info bg-info">{{{message}}}</h3>
                    {{/if}}
                    <form class="" action="/pubs/{{pub_id}}/beers/insert" method="post">
                        {{#if result}}
                        <button class="btn btn-info" type="submit" name="submit">Insert beers</button>
                        {{/if}}
                        <a class="btn btn-info" href="/pubs/{{pub_id}}/beers">Back to the pub menu</a>
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Type</th>
                                    <th>Degrees</th>
                                    <th>Into menu</th>
                                    <th>Price</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#each result}}
                                <tr class="beer_entry">
                                    <td>{{this.name}}</td>
                                    <td>{{this.type}}</td>
                                    <td>{{this.alcoholic_degree}}%</td>
                                    <td><input type="checkbox" name="beers" value="{{this.id}}"></td>
                                    <td><input class="text-muted" type="text" name="{{this.id}}" placeholder="price"></td>
                                </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </form>

                </div>

            </div>

        </div>

    </div>

    {{/extend}}

    {{#extend "stylesheets"}}
    <link rel='stylesheet' href='/stylesheets/cover.css' />
    {{/extend}}

    {{#extend "script"}}
    {{/extend}}
